# Little Computer People — TODO

**Tier: Silver.** 100 % coverage (61,815 tracked bytes, every routine,
table and variable described), `facts.md` with its live tests, every row of
`features.md` confirmed, live, differs or open with its search described,
`symbols.json` and `listing.json`, and the minisite built. All of it is
agent-written (`copy`: `agent-draft`).

## For Gold

A human goes through the minisite section by section: cuts what is dull,
rewrites what reads like an agent, expands what is interesting, and adds
what the agent missed. The contributor has already added three things by
watching the emulator (the TV, the turntable and the talking); more of
that kind of observation is what the page wants.

## Open questions worth a human's time

- The fourth piano piece (`$838D`): recorded in `work/music-piano-1.json`
  by the build; nobody has named it.
- What activity 113 depicts, and whether activity 24's white object is the
  newspaper: watch them in the emulator.
- The chooser's rule for requests that score 4 to 7 (facts.md, "Open
  questions and oddities"): a sad "light a fire" at 4 was not carried out,
  a happy one at 7 was.
- Which release the crack was made from. A disk or cassette image of the
  original would settle what the crack removed (the guestbook, the card
  games, the disk memory) and what was never in this version.

## Article ideas not built

- A player for the talking squawks, built from the sound table at `$D764`
  and the phrase tables at `$B7FE`.
- A stepper for an activity's state machine: pick an activity, see its
  phase table and step through the handlers.
- The dog: its 21 places, its bowl and its waits.
