# Mercenary — cheats

Pokes that change the game within its own parameters. Each one names the
variable it changes and whether it has been tested live. Untested pokes are
labelled as candidates.

| Effect | Poke | Status |
|---|---|---|
| Stand on the 08-08 lift patch (on foot at 08-08, then press E: down into the Colony Craft's hangar) | `$73` = `$72`, `$79` = `$72` (X and Y middle bytes); in VICE's monitor `> 73 72` and `> 79 72` | live |
